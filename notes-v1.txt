STRUKTUR PROJECT
movie-watchlist/
├── node_modules/
├── public/             image, icon, static assets
├── src/                Tempat semua source code React
│   └── app/            routing system Next.js
|      └── page.tsx     code pertama yang dirender saat buka localhost:3000
├── package.json
├── tsconfig.json
├── next.config.ts

STEP 1 : setup project
> npx create-next-app@latest movie-watchlist
√ Would you like to use the recommended Next.js defaults? » No, customize settings
√ Would you like to use TypeScript? ... No / Yes
√ Which linter would you like to use? » ESLint
√ Would you like to use React Compiler? ... No / Yes
√ Would you like to use Tailwind CSS? ... No / Yes
√ Would you like your code inside a `src/` directory? ... No / Yes
√ Would you like to use App Router? (recommended) ... No / Yes
√ Would you like to customize the import alias (`@/*` by default)? ... No / Yes
√ Would you like to include AGENTS.md to guide coding agents to write up-to-date Next.js code? ... No / Yes

=================== PART 1 - bagaimana frontend mengambil data ===================

STEP 2 - Hapus Template Next.js (src/app/page.tsx)
export default function HomePage() {
  return (
    <main>
      <h1>Movie Watchlist</h1>
    </main>
  );
}
- HomePage() -> react component
component adalah potongan ui yang reusable
- return () -> isi tampilan yang akan muncul

STEP 3 - Jalankan Project
> npm run dev

STEP 4 - Foldering project
src/
├── app/            page, route
├── features/       fitur aplikasi : watchlist, auth, profile
├── hooks/          reusable logic
├── lib/            helper, API utility, shared function
├── types/          TypeScript types

STEP 5 - Buat Feature pertama
src/features/watchlist

STEP 6 - Buat Type movie
src/features/watchlist/types/movie.ts
- Movie -> TypeScript Interface
interface digunakan untuk mendefinisikan bentuk data

STEP 7 - Buat API Service
src/features/watchlist/services/movie-api.ts
- movieApi -> API Service Layer
layer ini digunakan untuk komunikasi backend, fetch data, handle HTTP request

STEP 8 - Buat custom hook 
src/features/watchlist/hooks/use-movies.ts
- useMovies
semua React hook diawali kata use
Tujuan Hook adalah memisahkan logic dari UI
- const [movies, setMovies] 
untuk menyimpan data movie
- const [loading, setLoading]
untuk mengetahui masih loading atau sudah selesai
- useEffect(() => {})
jalankan sesuatu setelah component render
- [] -> dependency Array
artinya hanya dijalankan sekali

STEP 9 - Buat component UI
src/features/watchlist/components/movie-list.tsx
component ini tidak fetch langsung melainkan pakai hook

STEP 10 - Hubungkan ke page (src/app/page.tsx)
Tambahkan :
<MovieList />

STEP 11 - Jalankan Project
> npm run dev

=================== PART 2 - Form + State Flow React ===================
Di React: UI mengikuti state
User mengetik
    ↓
State berubah
    ↓
React re-render
    ↓
UI ikut berubah

STEP 12 - Tambahkan create movie
Buka src/features/watchlist/hooks/use-movies.ts
Tambahkan function addMovie
- setMovies((currentMovies) => ...)
ambil state lama lalu update

STEP 13 - Buat Movie Form 
src/features/watchlist/components/movie-form.tsx
- MovieFormProps adalah props
props = data yang dikirim dari parent (MovieList) ke child (MovieForm)

STEP 14 - Hubungkan MovieForm ke MovieList
Buka src/features/watchlist/components/movie-list.tsx
Tambahkan :
<MovieForm onAddMovie={addMovie} />

----------------------------------------------------
src\features\watchlist\components\movie-list.tsx
- const { movies, loading, addMovie } = useMovies();
MovieList memanggil hook useMovies()

src\features\watchlist\hooks\use-movie.ts
- useMovies()
addMovie didefinisikan sebagai function
hook useMovies() return addMovie()

src\features\watchlist\components\movie-list.tsx
- const { movies, loading, addMovie } = useMovies();
Sekarang MovieList punya function addMovie
- <MovieForm onAddMovie={addMovie} />
prop onAddMovie = function onAddMovie

src\features\watchlist\components\movie-form.tsx
- export function MovieForm({ onAddMovie }: MovieFormProps)
ambil prop onAddMovie (sama saja dengan function addMovie)
- onAddMovie({
    title,
    personalRating: rating,
  });
karena onAddMovie === addMovie, maka jalankan function addMovie() pada hook / useMovies()

src\features\watchlist\components\movie-list.tsx
- setMovies((currentMovies) => [newMovie, ...currentMovies]);
update state dan render UI ulang, movie baru muncul
----------------------------------------------------

STEP 15 - Jalankan Project
> npm run dev

=================== PART 3 - Delete Movie ===================
STEP 16 - Tambahkan deleteMovie()
Buka src/features/watchlist/hooks/use-movies.ts
- filter() -> array baru
ambil semua movie KECUALI movie yang id nya sama (id movie ingin dihapus)

STEP 17 - Return deleteMovie
Buka src/features/watchlist/hooks/use-movies.ts
Tambahkan :
deleteMovie,

STEP 18 - Ambil deleteMovie di component
Buka src\features\watchlist\components\movie-list.tsx
ubah menjadi const { movies, loading, addMovie, deleteMovie } = useMovies();

STEP 19 - Tambahkan Button delete 
Buka src\features\watchlist\components\movie-list.tsx
Tambahkan :
<button onClick={() => deleteMovie(movie.id)}>
  Delete
</button>

-----------------------------------------------------
- src\features\watchlist\components\movie-list.tsx
onClick={() => deleteMovie(movie.id)}
- src/features/watchlist/hooks/use-movies.ts
deleteMovie(movie.id)
setMovies(...)
--------------------------------------------------------

STEP 15 - Jalankan Project
> npm run dev


=================== PART 4 - Toggle Watched ===================
STEP 21 — Tambahkan toggleWatched()
Buka src/features/watchlist/hooks/use-movies.ts
Tambahkan toggleWatched()
- currentMovies.map(...) -> update item dalam array

STEP 22 — Return toggleWatched
Buka src/features/watchlist/hooks/use-movies.ts
Tambahkan:
toggleWatched,

STEP 23 — Ambil Di component
Buka src\features\watchlist\components\movie-list.tsx
Ubah menjadi const { movies, loading, addMovie, deleteMovie, toggleWatched } = useMovies();

STEP 24 — Tambahkan Button 
Buka src\features\watchlist\components\movie-list.tsx
Tambahkan :
<button onClick={() => toggleWatched(movie.id)}>
  {movie.watched ? "Watched" : "Unwatched"}
</button>
Ubah style title movie untuk membedakan flag sudah ditonton

=================== PART 4 - Edit Movie ===================
STEP 25 — Tambahkan updateMovie()
Buka src/features/watchlist/hooks/use-movies.ts
Tambahkan :
function updateMovie()
return updateMovie,

STEP 26 — Tambahkan editingMovieId
untuk tahu movie mana yang sedang diedit
Buka src\features\watchlist\components\movie-list.tsx
Tambahkan :
const [editingMovieId, setEditingMovieId] = useState<number | null>(null);

STEP 27 — Cari Movie yang diedit 
Tambahkan :
const editingMovie = movies.find((movie) => movie.id === editingMovieId);

STEP 28 — Update MovieFormProps
Buka src\features\watchlist\components\movie-form.tsx
ubah type menjadi :
type MovieFormProps = {
  onAddMovie: (input: {
    title: string;
    personalRating: number;
  }) => void;

  initialData?: {
    title: string;
    personalRating: number;
  };

  isEditing?: boolean;
};

STEP 29 — Update useState form
Buka src\features\watchlist\components\movie-form.tsx
ubah menjadi :
const [title, setTitle] = useState(
  initialData?.title || ""
);

const [rating, setRating] = useState(
  initialData?.personalRating || 0
);

STEP 30 — Update parameter Component 
Buka src\features\watchlist\components\movie-form.tsx
ubah menjadi :
export function MovieForm({ onAddMovie, initialData, isEditing }: MovieFormProps) {

STEP 31 — Update button text
Buka src\features\watchlist\components\movie-form.tsx
ubah menjadi :
{isEditing ? "Update Movie" : "Add Movie"}

STEP 32 — Hubungkan ke movie-list.tsx
Buka src\features\watchlist\components\movie-list.tsx
ubah menjad :
<MovieForm
  onAddMovie={(input) => {
    if (editingMovieId) {
      updateMovie(editingMovieId, input);
      setEditingMovieId(null);
    } else {
      addMovie(input);
    }
  }}
  initialData={editingMovie}
  isEditing={!!editingMovieId}
/>

STEP 33 — Tambahkan button edit 
Buka src\features\watchlist\components\movie-list.tsx
Tambahkan :
<button onClick={() => setEditingMovieId(movie.id)}>
  Edit
</button>

STEP 34 — Tambahkan useEffect
Buka src\features\watchlist\components\movie-form.tsx
Tambahkan 
useEffect(() => {
  if (initialData) {
    setTitle(initialData.title);
    setRating(initialData.personalRating);
  }
}, [initialData]);

------------------------------
Klik Edit (movie-list.tsx)
   ↓
editingMovieId berubah (movie-list.tsx)
   ↓
editingMovie ditemukan (movie-list.tsx)
   ↓
initialData dikirim ke form (movie-form.tsx)
   ↓
useEffect update state form (movie-form.tsx)
   ↓
input terisi (movie-form.tsx)
   ↓
submit (movie-list.tsx)
   ↓
updateMovie(5, data) (movie-form.tsx)
   ↓
movies state berubah / setEditingMovieId(null) (movie-form.tsx)
   ↓
React render ulang
   ↓
UI update



