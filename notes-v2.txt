-------------------------- PART 1 - custom hook reusable --------------------------
STEP 1 — Buat folder hook
Buat src/shared/hooks

STEP 2 — Buat file hook 
Buat src/shared/hooks/use-local-storage.ts
tujuan hook ini adalah walaupun refresh browser, state tetap ada

STEP 3 — Adjust hook
Buka src\features\watchlist\hooks\use-movie.ts
Ganti useState() menjadi useLocalStorage()

STEP 4 — IMPORT HOOK BARU
Buka src\features\watchlist\hooks\use-movie.ts
Tambahkan :
import { useLocalStorage } from "@/shared/hooks/use-local-storage";

STEP 5 — GANTI useState MOVIES
Buka src\features\watchlist\hooks\use-movie.ts
Ganti menjadi :
const [movies, setMovies] = useLocalStorage<Movie[]>("movies", []);
- browser akan menyimpan: localStorage["movies"]

STEP 6 - Jalankan Project -> movie masih hilang ketika refresh
> npm run dev

STEP 7 — UPDATE useEffect
Buka src\features\watchlist\hooks\use-movie.ts

Component render
↓
cek localStorage
↓
ambil data lama jika ada
↓
buat state React
↓
saat state berubah
↓
simpan lagi ke localStorage

-------------------------- PART 2 - context API / shared global state --------------------------
STEP 8 — BUAT FOLDER CONTEXT
Buat src/features/watchlist/context

STEP 9 — BUAT FILE CONTEXT
Buat src/features/watchlist/context/movie-context.tsx

STEP 10 — Isi Code file context 
Buat src/features/watchlist/context/movie-context.tsx
Context adalah tempat menyimpan shared state
Provider adalah pembungkus yang membagikan data ke seluruh component di dalamnya

- useMovieContext()
digunakan untuk mengambil data dari context
- const movieState = useMovies();
artinya context memakai hook useMovies

MovieProvider
   ↓
menyimpan movie state
   ↓
component di dalamnya
bisa akses movie state

STEP 11 — BUNGKUS APP DENGAN PROVIDER
Buka src/app/page.tsx
<MovieProvider></MovieProvider>
bagikan movie state ke semua component di dalamnya yaitu <MovieList />
Jadi MovieList sekarang: bisa akses Context

STEP 12 — GANTI useMovies MENJADI useMovieContext
Buka : src\features\watchlist\components\movie-list.tsx
const { movies, loading, addMovie, deleteMovie, toggleWatched, updateMovie, } = useMovieContext();
MovieList mengambil state global dari Context
